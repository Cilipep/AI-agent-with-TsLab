# Script Blocks Quick Reference

Use this only as a tiny reminder.
The authoritative graph-editing guide is `AGENTS-ScriptEditing.md`.

## Reminders

- edit scripts through `/ops`, not raw XML
- read `/explain` before rewiring
- formulas use `CodeName`, not `blockId`
- input indices are zero-based
- graph links and UI links are different things
- validate after graph edits

If you need more than these reminders, open `AGENTS-ScriptEditing.md`.